{%- liquid
    assign sort_by = results.sort_by | default: results.default_sort_by
    assign total_active_values = 0
    assign price_range_active = false
    if results.url
      assign results_url = results.url
    else 
      assign terms = results.terms | escape
      assign results_url = '?q=' | append: terms | append: '&options%5Bprefix%5D=last&sort_by=' | append: sort_by
    endif

    if enable_filtering
      assign price_range_active = false
      for filter in results.filters
          if filter.type == "price_range"
            if filter.min_value.value != nil or filter.max_value.value != nil
              assign price_range_active = true
            endif
          endif
          assign total_active_values = total_active_values | plus: filter.active_values.size
      endfor
    endif
    assign default_presentation = 'text'

  if settings.typography_preset == 'avant_garde'
			assign close_weight = settings.accent_icon_weight
			if settings.swap_fonts
					assign close_weight = settings.base_icon_weight
			endif
	else 
			assign close_weight =  settings.base_icon_weight
			if settings.swap_fonts
					assign close_weight = settings.accent_icon_weight
			endif
	endif

  if settings.typography_preset == 'classic' or settings.typography_preset == 'duet'
    assign weight_accordion_title = settings.accent_icon_weight
    if settings.swap_fonts
        assign weight_accordion_title = settings.base_icon_weight
    endif
  else 
    assign weight_accordion_title =  settings.base_icon_weight
    if settings.swap_fonts
        assign weight_accordion_title = settings.accent_icon_weight
    endif
  endif

  if settings.typography_preset == 'brutalist' or settings.typography_preset == 'duet' or settings.typography_preset == 'avant_garde' or settings.typography_preset == 'contrast'
    assign secondary_weight = settings.accent_icon_weight
    if settings.swap_fonts
        assign secondary_weight = settings.base_icon_weight
    endif
  else 
    assign secondary_weight =  settings.base_icon_weight
    if settings.swap_fonts
        assign secondary_weight = settings.accent_icon_weight
    endif
  endif

  if settings.typography_preset == 'avant_garde' or settings.typography_preset == 'duet'
			assign select_weight = settings.accent_icon_weight
			if settings.swap_fonts
					assign select_weight = settings.base_icon_weight
			endif
    else 
        assign select_weight =  settings.base_icon_weight
        if settings.swap_fonts
            assign select_weight = settings.accent_icon_weight
        endif
    endif
    if settings.typography_preset == 'custom'
      assign close_weight = settings.custom_icon_weight
      assign weight_accordion_title = settings.custom_icon_weight
      assign secondary_weight = settings.custom_icon_weight
      assign select_weight = settings.custom_icon_weight
    endif

    assign uppercase_body_s = false
    assign bolder_body_s = false
    if settings.typography_preset == 'custom'
        if settings.uppercase_body_s 
            assign uppercase_body_s = true
        endif
        if settings.bolder_font_body_s 
            assign bolder_body_s = true
        endif
    endif
-%}

{{ 'component-show-more-button.css' | asset_url | stylesheet_tag }}

{% style %}
    .facets__button-icon {
      height: {{ 20 | times: settings.desktop_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
      width: {{ 20 | times: settings.desktop_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
    }
    .results-count--minimal_uppercase, .results-count--classic, .results-count--avant_garde, .results-count--contrast {
      min-width: {{ 24 | times: settings.desktop_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
      height: {{ 24 | times: settings.desktop_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
    }
    .results-count--minimal_basic {
      min-width: {{ 16 | times: settings.desktop_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
      height: {{ 16 | times: settings.desktop_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
    }
    .results-count--brutalist, .results-count--minimal_heavy {
      min-width: {{ 28 | times: settings.desktop_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
      height: {{ 28 | times: settings.desktop_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
    }
    .results-count--duet {
      min-width: {{ 26 | times: settings.desktop_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
      height: {{ 26 | times: settings.desktop_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
    }
    .results-count--custom,
    .button-font--default .facets__button-icon {
      height: max(16px, calc(var(--body-s-font-size) * var(--body-s-line-height)));
      min-width: max(16px, calc(var(--body-s-font-size) * var(--body-s-line-height)));
    }
    .custom-type-preset .heading_xs .results-count--custom,
    .custom-type-preset .heading_xs_uppercase .results-count--custom,
    .custom-type-preset .heading_xs .facets__button-icon,
    .custom-type-preset .heading_xs_uppercase .facets__button-icon {
      height: max(16px, calc(var(--heading-xs-font-size) * var(--heading-xs-line-height)));
      min-width: max(16px, calc(var(--heading-xs-font-size) * var(--heading-xs-line-height)));
    }
    .custom-type-preset .body_l .results-count--custom,
    .custom-type-preset .body_l_uppercase .results-count--custom,
    .custom-type-preset .body_l .facets__button-icon,
    .custom-type-preset .body_l_uppercase .facets__button-icon {
      height: max(16px, calc(var(--body-l-font-size) * var(--body-l-line-height)));
      min-width: max(16px, calc(var(--body-l-font-size) * var(--body-l-line-height)));
    }
    .custom-type-preset .body_m .results-count--custom,
    .custom-type-preset .body_m_uppercase .results-count--custom,
    .custom-type-preset .body_m .facets__button-icon,
    .custom-type-preset .body_m_uppercase .facets__button-icon {
      height: max(16px, calc(var(--body-m-font-size) * var(--body-m-line-height)));
      min-width: max(16px, calc(var(--body-m-font-size) * var(--body-m-line-height)));
    }
    .custom-type-preset .label_font .results-count--custom,
    .custom-type-preset .label_font_uppercase .results-count--custom,
    .custom-type-preset .label_font .facets__button-icon,
    .custom-type-preset .label_font_uppercase .facets__button-icon {
      height: max(16px, calc(var(--label-font-size) * var(--label-line-height)));
      min-width: max(16px, calc(var(--label-font-size) * var(--label-line-height)));
    }
    

    @media screen and (max-width: 920px) {
      .facets__button-icon {
        height: {{ 20 | times: settings.mobile_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
        width:  {{ 20 | times: settings.mobile_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
      }
      .results-count--minimal_uppercase, .results-count--classic, .results-count--avant_garde, .results-count--contrast {
        min-width: {{ 24 | times: settings.mobile_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
        height: {{ 24 | times: settings.mobile_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
      }
      .results-count--minimal_basic {
        min-width: {{ 16 | times: settings.mobile_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
        height: {{ 16 | times: settings.mobile_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
      }
      .results-count--brutalist, .results-count--minimal_heavy {
        min-width: {{ 28 | times: settings.mobile_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
        height: {{ 28 | times: settings.mobile_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
      }
      .results-count--duet {
        min-width: {{ 26 | times: settings.mobile_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
        height: {{ 26 | times: settings.mobile_font_scale | divided_by: 100.0 | round | at_least: 16 }}px;
      }
    }
{% endstyle %}

{% if section.settings.desktop_filter_style == 'horizontal' %}
  <modal-dialog class="snippet-facets only-desktop snippet-facets--horizontal">
    <div class="facets-container-drawer facets-container-drawer--{{ settings.white_space }} {% if settings.show_lines %}lines{% endif %}">
      <facet-drawer class="mobile-facets__wrapper {% if settings.enable_buttons_zoom %}buttons-zoom{% endif %} {% if settings.center_text %}mobile-facets__wrapper--center{% endif %}">
          <div class="filter-form">
            <facet-filters-form>
              <div class="facet-filters-form-line">
                <div>
                  <div class="active-facets active-facets-desktop">
                    <div class="product-count label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" role="status">
                      <p class="product-count__text">
                        {%- liquid
                          if results.results_count
                            if results.results_count == 1
                              echo 'templates.search.results_with_count.one' | t: terms: results.terms, count: results.results_count
                            else
                              echo 'templates.search.results_with_count.other' | t: terms: results.terms, count: results.results_count
                            endif
                          elsif results.products_count == results.all_products_count
                            if results.products_count == 1
                              echo 'products.facets.product_count_simple.one' | t: count: results.products_count
                            else
                              echo 'products.facets.product_count_simple.other' | t: count: results.products_count
                            endif
                          else
                            if results.products_count == 1
                            echo 'products.facets.product_count.one' | t: product_count: results.products_count, count: results.all_products_count
                            else
                            echo 'products.facets.product_count.other' | t: product_count: results.products_count, count: results.all_products_count
                            endif
                          endif
                        -%}
                      </p>
                    </div>
                  </div>
                </div>
                <form id="FacetFiltersForm" class="mobile-facets">
                  <div class="mobile-facets__inner">
                    <div class="mobile-facets__main">
                      {%- if enable_filtering -%}
                        {%- for filter in results.filters -%}
                          {% liquid 
                            assign presentation = filter.presentation | default: default_presentation
                          %}
                          {% case filter.type %}
                          {% when 'boolean' or 'list' %}
                          {% liquid 
                            assign color_swatches_label_1 = settings.color_swatches_label_1 | downcase 
                            assign color_swatches_label_2 = settings.color_swatches_label_2 | downcase 
                            assign color_swatches_label_3 = settings.color_swatches_label_3 | downcase 
                            assign thumbnail_swatches_label_1 = settings.thumbnail_swatches_label_1 | downcase
                            assign thumbnail_swatches_label_2 = settings.thumbnail_swatches_label_2 | downcase
                            assign thumbnail_swatches_label_3 = settings.thumbnail_swatches_label_3 | downcase
                           
                            assign filter_label = filter.label | downcase
                            assign swatch = false
                            assign thumbnail = false

                            if presentation == 'image'
                                assign thumbnail = true
                            endif
                            if presentation == 'swatch'
                                assign swatch = true
                            endif

                            if presentation == 'text'
                              if filter_label == color_swatches_label_1 or filter_label == color_swatches_label_2 or filter_label == color_swatches_label_3
                                assign swatch = true
                              endif
                              if filter_label == thumbnail_swatches_label_1 or filter_label == thumbnail_swatches_label_2 or filter_label == thumbnail_swatches_label_3
                                assign thumbnail = true
                              endif
                              if swatch and thumbnail
                                assign swatch = false
                                assign thumbnail = true
                              endif
                            endif
                            
                            assign tooltip = false
                            if swatch and settings.color_swatches_layout == 'grid'
                              assign tooltip = true
                            elsif thumbnail and settings.show_thumbnail_name == false
                              assign tooltip = true
                            endif
                          %}
                            <accordion-block data-index="desktop-{{ forloop.index }}">
                              <div class="accordion-toggle no-js-hidden filter-group mobile-facets__details js-filter" id="Details-{{ filter.param_name | escape }}-{{ section.id }}" data-index="desktop-{{ forloop.index }}" tabindex="0">
                                <div class="mobile-facets__summary accordion__summary" role="button" tabindex="0">
                                  <span class="summary__title">
                                    <span class="summary__title-name filter-group-label filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">{{ filter.label | escape }}
                                      <div class="filter-group-icon filter-group-icon--plus">
                                        {% if weight_accordion_title == 'regular' %}
                                                {% render 'icon', icon: 'plus-regular' %}
                                        {% elsif weight_accordion_title == 'medium' %}
                                                {% render 'icon', icon: 'plus-medium' %}
                                        {% elsif weight_accordion_title == 'bold' %}
                                                {% render 'icon', icon: 'plus-bold' %}
                                        {% endif %}
                                      </div>
                                      <div class="filter-group-icon filter-group-icon--minus">
                                        {% if weight_accordion_title == 'regular' %}
                                                {% render 'icon', icon: 'minus-regular' %}
                                        {% elsif weight_accordion_title == 'medium' %}
                                                {% render 'icon', icon: 'minus-medium' %}
                                        {% elsif weight_accordion_title == 'bold' %}
                                                {% render 'icon', icon: 'minus-bold' %}
                                        {% endif %}
                                      </div>
                                    </span>
                                  </span>
                                </div>
                                <div class="accordion__panel mobile-facets__submenu filter-group-display">
                                  <div class="facets__header">
                                    <span class="facets__header-info label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %} dim">
                                      <span class="facets__selected no-js-hidden">
                                        {{- 'products.facets.filters_selected' | t: count: filter.active_values.size -}}
                                      </span>
                                    </span>
                                    <facet-remove>
                                      <a href="{{ filter.url_to_remove }}" class="facets__reset label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %} dim">
                                        {{ 'products.facets.reset' | t }}
                                      </a>
                                    </facet-remove>
                                  </div>
                                  {%- if filter.operator == 'AND' and section.settings.filter_and_operator_subtitle != blank -%}
                                    <span class="facets__and-helptext {% if settings.dim_text %}dim{% endif %} filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">
                                      {{ section.settings.filter_and_operator_subtitle }}
                                    </span>
                                  {%- endif -%}
                                  {% liquid 
                                    assign grid_view = false
                                    if swatch and settings.color_swatches_layout == 'grid'
                                      assign grid_view = true
                                    elsif thumbnail
                                      assign grid_view = true
                                    endif
                                  %}
                                  <ul class="accordion__content mobile-facets__list list-unstyled {% if grid_view %}filter-group-display__grid{% else %}filter-group-display__list{% endif %}" role="list">
                                    {%- for value in filter.values -%}
                                      {%- liquid
                                        assign is_color = false
                                        if swatch or thumbnail
                                          assign swatch_file_extension = 'png'
                                          assign swatch_trigger = 'products.product.color_swatch_tag' | t | downcase
                                        endif
                                        if presentation == 'text'
                                          assign color_file_name = value.value | replace: ' ', '_' | append: '.' | append: swatch_file_extension
                                          assign color_value = value.value | replace: ' ', '_' | downcase
                                          assign color_swatch_fallback = value.value | downcase | split: ' ' | last
                                          assign img_file = images[color_file_name] | downcase
                                          assign color_image = images[color_file_name] | split: '/' | last | file_img_url: '100x100' | prepend: 'https:' | split: '?' | first
                                        endif
                                        assign swatch_value = null
                                        if presentation == 'swatch'
                                          if value.swatch.image
                                            assign image_url = value.swatch.image | image_url: width: 50
                                            assign swatch_value = 'url(' | append: image_url | append: ')'
                                            assign swatch_focal_point = value.swatch.image.presentation.focal_point
                                          elsif value.swatch.color
                                            assign swatch_value = 'rgb(' | append: value.swatch.color.rgb | append: ')'
                                          endif
                                        endif
                                        if presentation == 'image'
                                          assign image_url = value.image | image_url: width: 300
                                          assign swatch_value = 'url(' | append: image_url | append: ')'
                                        endif
                                      -%}
                                      <li class="mobile-facets__item list-menu__item filter-group-display__list-item{% if thumbnail %} mobile-facets__item--thumbnail{% endif %}">
                                        <label for="Filter-{{ filter.label | escape }}-desktop-{{ forloop.index }}" class="mobile-facets__label{% if value.count == 0 and value.active == false %} mobile-facets__label--disabled{% endif %} filter-group-option input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %} {% if thumbnail %}filter-group-option--thumbnail{% endif %}" {% if tooltip %}title="{{ value.label | escape }}"{% endif %}>
                                          <input class="mobile-facets__checkbox {% if swatch %} mobile-facets__swatch{% elsif thumbnail %}{% if settings.thumbnail_image_ratio == 'original' %} ratio-square{% else %} ratio-{{ settings.thumbnail_image_ratio }}{% endif %} mobile-facets__thumbnail{% if settings.enable_shadow %} mobile-facets__thumbnail--shadow{% endif %}{% unless settings.show_thumbnail_name %} no-label{% endunless %}{% endif %}{% if thumbnail and filter.presentation != 'swatch' %} mobile-facets__thumbnail--options{% unless settings.show_thumbnail_name %} no-label{% endunless %}{% endif %}
                                            {% if presentation == 'swatch' or presentation == 'image' %} filter-from-app{% endif %}"
                                            type="checkbox"
                                            name="{{ value.param_name }}"
                                            value="{{ value.value }}"
                                            id="Filter-{{ filter.label | escape }}-desktop-{{ forloop.index }}"
                                            {% if value.active %}checked="checked"{% endif %}
                                            {% if value.count == 0 and value.active == false %}disabled="disabled"{% endif %}
                                            {%  if presentation == 'swatch' or presentation == 'image' and value.image %}style="--swatch--background: {{ swatch_value }};{{ value.swatch.color.first }};--swatch-focal-point: {% if swatch_focal_point %}{{ swatch_focal_point }}{% else %}center{% endif %};"{% else %}
                                            {% if swatch or grid_view %}style="{% if images[color_file_name] != blank and img_file contains color_value %}--swatch-background-image: url({{ color_image }});{% else %} --swatch-background-color: {{ color_swatch_fallback | default: 'white' }};{% endif %}"{% endif %}{% endif %}
                                          />
                                          {% if swatch and settings.color_swatches_layout == 'list' %}
                                            <span class="mobile-facets__swatch--svg mobile-facets__swatch--svg-list">{% render 'icon-accordion-2', icon: 'short-checkmark' %}</span>
                                          {% elsif swatch and settings.color_swatches_layout == 'grid' %}
                                            <span class="mobile-facets__swatch--svg mobile-facets__swatch--svg-grid">{% render 'icon-accordion-2', icon: 'swatch-checkmark' %}</span>
                                          {% endif %}
                                          {% unless tooltip %}<span class="swatch__label">{{ value.label | escape }} ({{ value.count }})</span>{% endunless %}
                                        </label>
                                      </li>
                                    {%- endfor -%}
                                  </ul>
                                </div>
                              </div>
                            </accordion-block>
                          {% when 'price_range' %}
                            <accordion-block data-index="desktop-{{ forloop.index }}">
                              {% liquid
                                assign currencies_using_comma_decimals = 'ANG,ARS,BRL,BYN,BYR,CLF,CLP,COP,CRC,CZK,DKK,EUR,HRK,HUF,IDR,ISK,MZN,NOK,PLN,RON,RUB,SEK,TRY,USD,UYU,VES,VND' | split: ','
                                assign uses_comma_decimals = false
                                if currencies_using_comma_decimals contains cart.currency.iso_code
                                  assign uses_comma_decimals = true
                                endif
                              %}
                              <div class="accordion-toggle no-js-hidden filter-group mobile-facets__details js-filter" id="Details-{{ filter.param_name | escape }}-{{ section.id }}" data-index="desktop-{{ forloop.index }}" tabindex="0">
                                <div class="mobile-facets__summary accordion__summary" role="button">
                                  <span class="summary__title">
                                    <span class="summary__title-name filter-group-label filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">{{ filter.label | escape }}
                                    <div class="filter-group-icon filter-group-icon--plus">
                                      {% if weight_accordion_title == 'regular' %}
                                              {% render 'icon', icon: 'plus-regular' %}
                                      {% elsif weight_accordion_title == 'medium' %}
                                              {% render 'icon', icon: 'plus-medium' %}
                                      {% elsif weight_accordion_title == 'bold' %}
                                              {% render 'icon', icon: 'plus-bold' %}
                                      {% endif %}
                                    </div>
                                    <div class="filter-group-icon filter-group-icon--minus">
                                      {% if weight_accordion_title == 'regular' %}
                                              {% render 'icon', icon: 'minus-regular' %}
                                      {% elsif weight_accordion_title == 'medium' %}
                                              {% render 'icon', icon: 'minus-medium' %}
                                      {% elsif weight_accordion_title == 'bold' %}
                                              {% render 'icon', icon: 'minus-bold' %}
                                      {% endif %}
                                    </div>
                                    </span>
                                  </span>
                                </div>
                                <div class="accordion__panel mobile-facets__submenu">
                                  <div class="facets__header">
                                    {%- assign max_price_amount = filter.range_max | money | strip_html | escape -%}
                                    <span class="facets__selected no-js-hidden label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %} dim">
                                      {{- 'products.facets.max_price' | t: price: max_price_amount -}}
                                    </span>
                                    <facet-remove>
                                      <a href="{{ filter.url_to_remove }}" class="facets__reset label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %} dim">
                                        {{ 'products.facets.reset' | t }}
                                      </a>
                                    </facet-remove>
                                  </div>
                                  <div class="facets__price">
                                    <div class="fields">
                                      <div class="field">
                                        <label class="field__label filter-group-option label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="Desktop-Filter-{{ filter.label | escape }}-GTE">{{ 'products.facets.from' | t }}</label>
                                        <div class="field__item" dir="ltr">
                                          <span class="field__currency input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">{{ cart.currency.symbol }}</span>
                                          <input class="field__input input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}"
                                            name="{{ filter.min_value.param_name }}"
                                            id="Desktop-Filter-{{ filter.label | escape }}-GTE"
                                            type="number"
                                            dir="ltr"
                                            placeholder="0"
                                            min="0"
                                            step="0.01"
                                            max="{{ filter.range_max | divided_by: 100.0 | ceil }}"
                                            {%- if filter.min_value.value -%}
                                              {%- if uses_comma_decimals %}
                                                value="{{ filter.min_value.value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"
                                              {%- else -%}
                                                value="{{ filter.min_value.value | money_without_currency | replace: ',', '' }}"
                                              {% endif -%}
                                            {%- endif -%}
                                          />
                                        </div>
                                      </div>
                                      <div class="field">
                                        <label class="field__label filter-group-option label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="Desktop-Filter-{{ filter.label | escape }}-LTE">{{ 'products.facets.to' | t }}</label>
                                          <div class="field__item" dir="ltr">
                                            <span class="field__currency input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">{{ cart.currency.symbol }}</span>
                                            <input class="field__input input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}"
                                              name="{{ filter.max_value.param_name }}"
                                              id="Desktop-Filter-{{ filter.label | escape }}-LTE"
                                              type="number"
                                              dir="ltr"
                                              placeholder="{{ filter.range_max | divided_by: 100.0 | ceil }}"
                                              min="0"
                                              step="0.01"
                                              max="{{ filter.range_max | divided_by: 100.0 | ceil }}"
                                              {%- if filter.max_value.value -%}
                                                {%- if uses_comma_decimals %}
                                                  value="{{ filter.max_value.value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"
                                                {%- else -%}
                                                  value="{{ filter.max_value.value | money_without_currency | replace: ',', '' }}"
                                                {% endif -%}
                                              {%- endif -%}
                                            />
                                          </div>
                                      </div>
                                    </div>
                                  </div>
                                </div>
                              </div>
                            </accordion-block>
                          {% endcase %}
                        {%- endfor -%}
                      {%- endif -%}
                      {%- if enable_sorting -%}
                        <div class="mobile-facets__details js-filter{% if filter_type == 'drawer' %} medium-hide large-up-hide{% endif %}" id="Details-{{ filter.param_name | escape }}-{{ section.id }}" data-index="desktop-{{ forloop.index }}">
                          <div class="mobile-facets__summary">
                            <div class="mobile-facets__sort">
                              <label class="filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="SortBy-desktop">{{ 'products.facets.sort_by_label' | t }}</label>
                              <div class="select">
                                <select name="sort_by" class="select__select input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" id="SortBy-desktop" aria-describedby="a11y-refresh-page-message">
                                  {%- for option in results.sort_options -%}
                                    <option value="{{ option.value | escape }}"{% if option.value == sort_by %} selected="selected"{% endif %}>{{ option.name | escape }}</option>
                                  {%- endfor -%}
                                </select>
                                <div class="select-icon">
                                  {% if select_weight == 'regular' %}
                                          {% render 'icon', icon: 'close-chevron-regular' %}
                                  {% elsif select_weight == 'medium' %}
                                          {% render 'icon', icon: 'close-chevron-medium' %}
                                  {% elsif select_weight == 'bold' %}
                                          {% render 'icon', icon: 'close-chevron-bold' %}
                                  {% endif %}
                              </div>  
                              </div>
                            </div>
                          </div>
                        </div>
                      {%- endif -%}
                    </div>
                    {%- if search.performed -%}<input type="hidden" name="q" value="{{ search.terms | escape }}"/>{%- endif -%}
                    {%- if collection.current_vendor or collection.current_type -%}<input type="hidden" name="q" value="{{ collection.current_vendor | escape }}{{ collection.current_type | escape }}"/>{%- endif -%}
                  </div>
                  {%- if results.terms -%}
                    <input type="hidden" name="q" value="{{ results.terms | escape }}"/>
                    <input name="options[prefix]" type="hidden" value="last"/>
                  {%- endif -%}
                  {%- if results.current_vendor or results.current_type -%}
                    <input type="hidden" name="q" value="{{ results.current_vendor | escape }}{{ results.current_type | escape }}"/>
                  {%- endif -%}
                  <div class="facets__wrapper">
                    <div class="sorting">
                      <div class="facet-filters__field">
                        <label class="filter-group-label filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="SortBy-Desktop">{{ 'products.facets.sort_by_label' | t }}</label>
                        <div class="select">
                          {%- assign sort_by = results.sort_by | default: results.default_sort_by -%}
                          <select name="sort_by" class="facet-filters__sort select__select caption-large sorting__select input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" id="SortBy-Desktop" aria-describedby="a11y-refresh-page-message">
                            {%- for option in results.sort_options -%}
                              <option value="{{ option.value | escape }}"{% if option.value == sort_by %} selected="selected"{% endif %}>{{ option.name | escape }}</option>
                            {%- endfor -%}
                          </select>
                          <div class="select-icon">
                            {% if select_weight == 'regular' %}
                                    {% render 'icon', icon: 'close-chevron-regular' %}
                            {% elsif select_weight == 'medium' %}
                                    {% render 'icon', icon: 'close-chevron-medium' %}
                            {% elsif select_weight == 'bold' %}
                                    {% render 'icon', icon: 'close-chevron-bold' %}
                            {% endif %}
                        </div>  
                        </div>
                      </div>
                    </div>
                  </div>
                </form>
              </div>
              <div class="active-facets active-facets--result">
                {%- for filter in results.filters -%}
                  {%- for value in filter.active_values -%}
                    <facet-remove>
                      <a class="active-facets__button hover-opacity button--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" href="{{ value.url_to_remove }}">
                        {{ filter.label | escape }}: {{ value.label | escape }}
                        {% if secondary_weight == 'regular' %}
                            {% render 'icon', icon: 'cross-regular' %}
                        {% elsif secondary_weight == 'medium' %}
                            {% render 'icon', icon: 'cross-medium' %}
                        {% elsif secondary_weight == 'bold' %}
                            {% render 'icon', icon: 'cross-bold' %}
                        {% endif %}
                      </a>
                    </facet-remove>
                  {%- endfor -%}
                  {% if filter.type == "price_range" %}
                    {%- if filter.min_value.value != nil or filter.max_value.value != nil -%}
                      <facet-remove>
                        <a class="active-facets__button hover-opacity button--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" href="{{ filter.url_to_remove }}">
                          {{ filter.label | escape }}: <span class="ltr-text">{% if filter.min_value.value != nil %}{{ filter.min_value.value | money_without_trailing_zeros }}{% else %}{{ 0 | money_without_trailing_zeros }}{% endif %}—{% if filter.max_value.value != nil %}{{ filter.max_value.value | money_without_trailing_zeros }}{% else %}{{ filter.range_max | money_without_trailing_zeros }}{% endif %}</span>
                          {% if secondary_weight == 'regular' %}
                              {% render 'icon', icon: 'cross-regular' %}
                          {% elsif secondary_weight == 'medium' %}
                              {% render 'icon', icon: 'cross-medium' %}
                          {% elsif secondary_weight == 'bold' %}
                              {% render 'icon', icon: 'cross-bold' %}
                          {% endif %}
                        </a>
                      </facet-remove>
                    {%- endif -%}
                  {% endif %}
                {%- endfor -%}
                <facet-remove class="facets__clear-all">
                  <a href="{{ results_url }}" class="active-facets__button hover-opacity active-facets__button--clear dim button--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">
                    {{ 'products.facets.clear_all' | t }}
                  </a>
                </facet-remove>
              </div>
              <div class="results-toast results-toast--horizontal label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" role="status">
                <p class="results-toast__text">
                  {%- liquid
                    if results.results_count
                      echo 'templates.search.results_with_count' | t: terms: results.terms, count: results.results_count
                    elsif results.products_count == results.all_products_count
                      if results.products_count == 1
                      echo 'products.facets.found_count_simple.one' | t: count: results.products_count
                      else 
                      echo 'products.facets.found_count_simple.other' | t: count: results.products_count
                      endif
                    else
                      if results.products_count == 1
                      echo 'products.facets.found_count.one' | t: product_count: results.products_count, count: results.all_products_count
                      else
                      echo 'products.facets.found_count.other' | t: product_count: results.products_count, count: results.all_products_count
                      endif
                    endif
                  -%}
                </p>
              </div>
            </facet-filters-form>
          </div>
      </facet-drawer>
    </div>
  </modal-dialog>
{% endif %}


{% if settings.navigation_layout == 'header_based' and section.settings.desktop_filter_style == 'vertical' %}
  <modal-dialog class="snippet-facets only-desktop snippet-facets--vertical snippet-facets--vertical-2">
    <div class="facets-container-drawer facets-container-drawer--{{ settings.white_space }} {% if settings.show_lines %}lines{% endif %}">
      <facet-drawer class="mobile-facets__wrapper {% if settings.enable_buttons_zoom %}buttons-zoom{% endif %} {% if settings.center_text %}mobile-facets__wrapper--center{% endif %}">
          <div class="filter-form">
            <facet-filters-form>
                <form id="FacetFiltersForm-2" class="mobile-facets">
                    <div class="active-facets active-facets-desktop active-facets-desktop-2">
                      <div class="product-count label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" role="status">
                        <p class="product-count__text">
                          {%- liquid
                            if results.results_count
                              if results.results_count == 1
                                echo 'templates.search.results_with_count.one' | t: terms: results.terms, count: results.results_count
                              else
                                echo 'templates.search.results_with_count.other' | t: terms: results.terms, count: results.results_count
                              endif
                            elsif results.products_count == results.all_products_count
                              if results.products_count == 1
                                echo 'products.facets.product_count_simple.one' | t: count: results.products_count
                              else
                                echo 'products.facets.product_count_simple.other' | t: count: results.products_count
                              endif
                            else
                              if results.products_count == 1
                              echo 'products.facets.product_count.one' | t: product_count: results.products_count, count: results.all_products_count
                              else
                              echo 'products.facets.product_count.other' | t: product_count: results.products_count, count: results.all_products_count
                              endif
                            endif
                          -%}
                        </p>
                      </div>
                    </div>
                    <div class="mobile-facets__inner">
                      <div class="mobile-facets__main">
                        {%- if enable_filtering -%}
                          {%- for filter in results.filters -%}
                            {% liquid 
                              assign presentation = filter.presentation | default: default_presentation
                            %}
                            {% case filter.type %}
                            {% when 'boolean' or 'list' %}
                              {% liquid 
                                assign color_swatches_label_1 = settings.color_swatches_label_1 | downcase 
                                assign color_swatches_label_2 = settings.color_swatches_label_2 | downcase 
                                assign color_swatches_label_3 = settings.color_swatches_label_3 | downcase 
                                assign thumbnail_swatches_label_1 = settings.thumbnail_swatches_label_1 | downcase
                                assign thumbnail_swatches_label_2 = settings.thumbnail_swatches_label_2 | downcase
                                assign thumbnail_swatches_label_3 = settings.thumbnail_swatches_label_3 | downcase
                               
                                assign filter_label = filter.label | downcase
                                assign swatch = false
                                assign thumbnail = false
    
                                if presentation == 'image'
                                    assign thumbnail = true
                                endif
                                if presentation == 'swatch'
                                    assign swatch = true
                                endif
    
                                if presentation == 'text'
                                  if filter_label == color_swatches_label_1 or filter_label == color_swatches_label_2 or filter_label == color_swatches_label_3
                                    assign swatch = true
                                  endif
                                  if filter_label == thumbnail_swatches_label_1 or filter_label == thumbnail_swatches_label_2 or filter_label == thumbnail_swatches_label_3
                                    assign thumbnail = true
                                  endif
                                  if swatch and thumbnail
                                    assign swatch = false
                                    assign thumbnail = true
                                  endif
                                endif
                                
                                assign tooltip = false
                                if swatch and settings.color_swatches_layout == 'grid'
                                  assign tooltip = true
                                elsif thumbnail and settings.show_thumbnail_name == false
                                  assign tooltip = true
                                endif
                              %}
                              <accordion-block data-index="desktop-{{ forloop.index }}">
                                <div class="accordion-toggle no-js-hidden filter-group mobile-facets__details js-filter" id="Details-{{ filter.param_name | escape }}-{{ section.id }}" data-index="desktop-{{ forloop.index }}" tabindex="0">
                                  <div class="mobile-facets__summary accordion__summary" role="button" tabindex="0">
                                    <span class="summary__title">
                                      <span class="summary__title-name filter-group-label filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">
                                        <span class="mobile-facets__title">{{ filter.label | escape }}</span>
                                        <div class="filter-group-icon filter-group-icon--plus">
                                          {% if weight_accordion_title == 'regular' %}
                                                  {% render 'icon', icon: 'plus-regular' %}
                                          {% elsif weight_accordion_title == 'medium' %}
                                                  {% render 'icon', icon: 'plus-medium' %}
                                          {% elsif weight_accordion_title == 'bold' %}
                                                  {% render 'icon', icon: 'plus-bold' %}
                                          {% endif %}
                                        </div>
                                        <div class="filter-group-icon filter-group-icon--minus">
                                          {% if weight_accordion_title == 'regular' %}
                                                  {% render 'icon', icon: 'minus-regular' %}
                                          {% elsif weight_accordion_title == 'medium' %}
                                                  {% render 'icon', icon: 'minus-medium' %}
                                          {% elsif weight_accordion_title == 'bold' %}
                                                  {% render 'icon', icon: 'minus-bold' %}
                                          {% endif %}
                                        </div>
                                      </span>
                                    </span>
                                  </div>
                                  <div class="accordion__panel mobile-facets__submenu filter-group-display">
                                    {%- if filter.operator == 'AND' and section.settings.filter_and_operator_subtitle != blank -%}
                                      <span class="facets__and-helptext {% if settings.dim_text %}dim{% endif %} filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">
                                        {{ section.settings.filter_and_operator_subtitle }}
                                      </span>
                                    {%- endif -%}
                                    {% liquid 
                                      assign grid_view = false
                                      if swatch and settings.color_swatches_layout == 'grid'
                                        assign grid_view = true
                                      elsif thumbnail
                                        assign grid_view = true
                                      endif
                                    %}
                                    <ul class="accordion__content mobile-facets__list list-unstyled {% if grid_view %}filter-group-display__grid{% else %}filter-group-display__list{% endif %}" role="list">
                                      {%- for value in filter.values -%}
                                        {%- liquid
                                          assign is_color = false
                                          if swatch or thumbnail
                                            assign swatch_file_extension = 'png'
                                            assign swatch_trigger = 'products.product.color_swatch_tag' | t | downcase
                                          endif
                                          if presentation == 'text'
                                            assign color_file_name = value.value | replace: ' ', '_' | append: '.' | append: swatch_file_extension
                                            assign color_value = value.value | replace: ' ', '_' | downcase
                                            assign color_swatch_fallback = value.value | downcase | split: ' ' | last
                                            assign img_file = images[color_file_name] | downcase
                                            assign color_image = images[color_file_name] | split: '/' | last | file_img_url: '100x100' | prepend: 'https:' | split: '?' | first
                                          endif
                                          assign swatch_value = null
                                          if presentation == 'swatch'
                                            if value.swatch.image
                                              assign image_url = value.swatch.image | image_url: width: 50
                                              assign swatch_value = 'url(' | append: image_url | append: ')'
                                              assign swatch_focal_point = value.swatch.image.presentation.focal_point
                                            elsif value.swatch.color
                                              assign swatch_value = 'rgb(' | append: value.swatch.color.rgb | append: ')'
                                            endif
                                          endif
                                          if presentation == 'image'
                                            assign image_url = value.image | image_url: width: 300
                                            assign swatch_value = 'url(' | append: image_url | append: ')'
                                          endif
                                        -%}
                                        <li class="mobile-facets__item list-menu__item filter-group-display__list-item{% if thumbnail %} mobile-facets__item--thumbnail{% endif %}">
                                          <label for="Filter-{{ filter.label | escape }}-desktop-{{ forloop.index }}" class="mobile-facets__label{% if value.count == 0 and value.active == false %} mobile-facets__label--disabled{% endif %} filter-group-option input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %} {% if thumbnail %}filter-group-option--thumbnail{% endif %}" {% if tooltip %}title="{{ value.label | escape }}"{% endif %}>
                                            <input class="mobile-facets__checkbox {% if swatch %} mobile-facets__swatch{% elsif thumbnail %}{% if settings.thumbnail_image_ratio == 'original' %} ratio-square{% else %} ratio-{{ settings.thumbnail_image_ratio }}{% endif %} mobile-facets__thumbnail{% if settings.enable_shadow %} mobile-facets__thumbnail--shadow{% endif %}{% unless settings.show_thumbnail_name %} no-label{% endunless %}{% endif %}{% if thumbnail and filter.presentation != 'swatch' %} mobile-facets__thumbnail--options{% unless settings.show_thumbnail_name %} no-label{% endunless %}{% endif %}
                                              {% if presentation == 'swatch' or presentation == 'image' %} filter-from-app{% endif %} "
                                              type="checkbox"
                                              name="{{ value.param_name }}"
                                              value="{{ value.value }}"
                                              id="Filter-{{ filter.label | escape }}-desktop-{{ forloop.index }}"
                                              {% if value.active %}checked="checked"{% endif %}
                                              {% if value.count == 0 and value.active == false %}disabled="disabled"{% endif %}
                                              {%  if presentation == 'swatch' or presentation == 'image' and value.image %}style="--swatch--background: {{ swatch_value }};{{ value.swatch.color.first }};--swatch-focal-point: {% if swatch_focal_point %}{{ swatch_focal_point }}{% else %}center{% endif %};"{% else %}
                                              {% if swatch or grid_view %}style="{% if images[color_file_name] != blank and img_file contains color_value %}--swatch-background-image: url({{ color_image }});{% else %} --swatch-background-color: {{ color_swatch_fallback | default: 'white' }};{% endif %}"{% endif %}{% endif %}
                                            />
                                            {% if swatch and settings.color_swatches_layout == 'list' %}
                                              <span class="mobile-facets__swatch--svg mobile-facets__swatch--svg-list">{% render 'icon-accordion-2', icon: 'short-checkmark' %}</span>
                                            {% elsif swatch and settings.color_swatches_layout == 'grid' %}
                                              <span class="mobile-facets__swatch--svg mobile-facets__swatch--svg-grid">{% render 'icon-accordion-2', icon: 'swatch-checkmark' %}</span>
                                            {% endif %}
                                            {% unless tooltip %}<span class="swatch__label">{{ value.label | escape }} ({{ value.count }})</span>{% endunless %}
                                          </label>
                                        </li>
                                      {%- endfor -%}
                                    </ul>
                                  </div>
                                </div>
                              </accordion-block>
                            {% when 'price_range' %}
                              <accordion-block data-index="desktop-{{ forloop.index }}">
                                {% liquid
                                  assign currencies_using_comma_decimals = 'ANG,ARS,BRL,BYN,BYR,CLF,CLP,COP,CRC,CZK,DKK,EUR,HRK,HUF,IDR,ISK,MZN,NOK,PLN,RON,RUB,SEK,TRY,USD,UYU,VES,VND' | split: ','
                                  assign uses_comma_decimals = false
                                  if currencies_using_comma_decimals contains cart.currency.iso_code
                                    assign uses_comma_decimals = true
                                  endif
                                %}
                                <div class="accordion-toggle no-js-hidden filter-group mobile-facets__details js-filter" id="Details-{{ filter.param_name | escape }}-{{ section.id }}" data-index="desktop-{{ forloop.index }}" tabindex="0">
                                  <div class="mobile-facets__summary accordion__summary" role="button">
                                    <span class="summary__title">
                                      <span class="summary__title-name filter-group-label filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">{{ filter.label | escape }}
                                      <div class="filter-group-icon filter-group-icon--plus">
                                        {% if weight_accordion_title == 'regular' %}
                                                {% render 'icon', icon: 'plus-regular' %}
                                        {% elsif weight_accordion_title == 'medium' %}
                                                {% render 'icon', icon: 'plus-medium' %}
                                        {% elsif weight_accordion_title == 'bold' %}
                                                {% render 'icon', icon: 'plus-bold' %}
                                        {% endif %}
                                      </div>
                                      <div class="filter-group-icon filter-group-icon--minus">
                                        {% if weight_accordion_title == 'regular' %}
                                                {% render 'icon', icon: 'minus-regular' %}
                                        {% elsif weight_accordion_title == 'medium' %}
                                                {% render 'icon', icon: 'minus-medium' %}
                                        {% elsif weight_accordion_title == 'bold' %}
                                                {% render 'icon', icon: 'minus-bold' %}
                                        {% endif %}
                                      </div>
                                      </span>
                                    </span>
                                  </div>
                                  <div class="accordion__panel mobile-facets__submenu">
                                    <div class="facets__price">
                                      <div class="fields">
                                        <div class="field">
                                          <label class="field__label filter-group-option label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="Desktop-Filter-{{ filter.label | escape }}-GTE">{{ 'products.facets.from' | t }}</label>
                                          <div class="field__item" dir="ltr">
                                            <span class="field__currency input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">{{ cart.currency.symbol }}</span>
                                            <input class="field__input input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}"
                                              name="{{ filter.min_value.param_name }}"
                                              id="Desktop-Filter-{{ filter.label | escape }}-GTE"
                                              type="number"
                                              dir="ltr"
                                              placeholder="0"
                                              min="0"
                                              step="0.01"
                                              max="{{ filter.range_max | divided_by: 100.0 | ceil }}"
                                              {%- if filter.min_value.value -%}
                                                {%- if uses_comma_decimals %}
                                                  value="{{ filter.min_value.value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"
                                                {%- else -%}
                                                  value="{{ filter.min_value.value | money_without_currency | replace: ',', '' }}"
                                                {% endif -%}
                                              {%- endif -%}
                                            />
                                          </div>
                                        </div>
                                        <div class="field">
                                          <label class="field__label filter-group-option label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="Desktop-Filter-{{ filter.label | escape }}-LTE">{{ 'products.facets.to' | t }}</label>
                                            <div class="field__item" dir="ltr">
                                              <span class="field__currency input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">{{ cart.currency.symbol }}</span>
                                              <input class="field__input input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}"
                                                name="{{ filter.max_value.param_name }}"
                                                id="Desktop-Filter-{{ filter.label | escape }}-LTE"
                                                type="number"
                                                dir="ltr"
                                                placeholder="{{ filter.range_max | divided_by: 100.0 | ceil }}"
                                                min="0"
                                                step="0.01"
                                                max="{{ filter.range_max | divided_by: 100.0 | ceil }}"
                                                {%- if filter.max_value.value -%}
                                                  {%- if uses_comma_decimals %}
                                                    value="{{ filter.max_value.value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"
                                                  {%- else -%}
                                                    value="{{ filter.max_value.value | money_without_currency | replace: ',', '' }}"
                                                  {% endif -%}
                                                {%- endif -%}
                                              />
                                            </div>
                                        </div>
                                      </div>
                                    </div>
                                  </div>
                                </div>
                              </accordion-block>
                            {% endcase %}
                          {%- endfor -%}
                        {%- endif -%}
                        {%- if enable_sorting -%}
                          <div class="mobile-facets__details js-filter{% if filter_type == 'drawer' %} medium-hide large-up-hide{% endif %}" id="Details-{{ filter.param_name | escape }}-{{ section.id }}" data-index="desktop-{{ forloop.index }}">
                            <div class="mobile-facets__summary">
                              <div class="mobile-facets__sort">
                                <label class="filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="SortBy-desktop">{{ 'products.facets.sort_by_label' | t }}</label>
                                <div class="select">
                                  <select name="sort_by" class="select__select input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" id="SortBy-desktop" aria-describedby="a11y-refresh-page-message">
                                    {%- for option in results.sort_options -%}
                                      <option value="{{ option.value | escape }}"{% if option.value == sort_by %} selected="selected"{% endif %}>{{ option.name | escape }}</option>
                                    {%- endfor -%}
                                  </select>
                                  <div class="select-icon">
                                    {% if select_weight == 'regular' %}
                                            {% render 'icon', icon: 'close-chevron-regular' %}
                                    {% elsif select_weight == 'medium' %}
                                            {% render 'icon', icon: 'close-chevron-medium' %}
                                    {% elsif select_weight == 'bold' %}
                                            {% render 'icon', icon: 'close-chevron-bold' %}
                                    {% endif %}
                                </div>  
                                </div>
                              </div>
                            </div>
                          </div>
                        {%- endif -%}
                      </div>
                      {%- if search.performed -%}<input type="hidden" name="q" value="{{ search.terms | escape }}"/>{%- endif -%}
                      {%- if collection.current_vendor or collection.current_type -%}<input type="hidden" name="q" value="{{ collection.current_vendor | escape }}{{ collection.current_type | escape }}"/>{%- endif -%}
                    </div>
                  {%- if results.terms -%}
                    <input type="hidden" name="q" value="{{ results.terms | escape }}"/>
                    <input name="options[prefix]" type="hidden" value="last"/>
                  {%- endif -%}
                  {%- if results.current_vendor or results.current_type -%}
                    <input type="hidden" name="q" value="{{ results.current_vendor | escape }}{{ results.current_type | escape }}"/>
                  {%- endif -%}
                  <div class="facet-filters-form-line">
                    <div class="active-facets active-facets--result active-facets--result-2">
                      {%- for filter in results.filters -%}
                        {%- for value in filter.active_values -%}
                          <facet-remove>
                            <a class="active-facets__button hover-opacity button--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" href="{{ value.url_to_remove }}">
                              {{ filter.label | escape }}: {{ value.label | escape }}
                              {% if secondary_weight == 'regular' %}
                                  {% render 'icon', icon: 'cross-regular' %}
                              {% elsif secondary_weight == 'medium' %}
                                  {% render 'icon', icon: 'cross-medium' %}
                              {% elsif secondary_weight == 'bold' %}
                                  {% render 'icon', icon: 'cross-bold' %}
                              {% endif %}
                            </a>
                          </facet-remove>
                        {%- endfor -%}
                        {% if filter.type == "price_range" %}
                          {%- if filter.min_value.value != nil or filter.max_value.value != nil -%}
                            <facet-remove>
                              <a class="active-facets__button hover-opacity button--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" href="{{ filter.url_to_remove }}">
                                {{ filter.label | escape }}: <span class="ltr-text">{% if filter.min_value.value != nil %}{{ filter.min_value.value | money_without_trailing_zeros }}{% else %}{{ 0 | money_without_trailing_zeros }}{% endif %}—{% if filter.max_value.value != nil %}{{ filter.max_value.value | money_without_trailing_zeros }}{% else %}{{ filter.range_max | money_without_trailing_zeros }}{% endif %}</span>
                                {% if secondary_weight == 'regular' %}
                                    {% render 'icon', icon: 'cross-regular' %}
                                {% elsif secondary_weight == 'medium' %}
                                    {% render 'icon', icon: 'cross-medium' %}
                                {% elsif secondary_weight == 'bold' %}
                                    {% render 'icon', icon: 'cross-bold' %}
                                {% endif %}
                              </a>
                            </facet-remove>
                          {%- endif -%}
                        {% endif %}
                      {%- endfor -%}
                      <facet-remove class="facets__clear-all">
                        <a href="{{ results_url }}" class="active-facets__button hover-opacity active-facets__button--clear dim button--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">
                          {{ 'products.facets.clear_all' | t }}
                        </a>
                      </facet-remove>
                    </div>
                    <div class="facets__wrapper">
                      <div class="sorting">
                        <div class="facet-filters__field">
                          <label class="filter-group-label filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="SortBy-desktop">{{ 'products.facets.sort_by_label' | t }}</label>
                          <div class="select">
                            {%- assign sort_by = results.sort_by | default: results.default_sort_by -%}
                            <select name="sort_by" class="facet-filters__sort select__select caption-large sorting__select input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" id="SortBy-desktop" aria-describedby="a11y-refresh-page-message">
                              {%- for option in results.sort_options -%}
                                <option value="{{ option.value | escape }}"{% if option.value == sort_by %} selected="selected"{% endif %}>{{ option.name | escape }}</option>
                              {%- endfor -%}
                            </select>
                            <div class="select-icon">
                              {% if select_weight == 'regular' %}
                                      {% render 'icon', icon: 'close-chevron-regular' %}
                              {% elsif select_weight == 'medium' %}
                                      {% render 'icon', icon: 'close-chevron-medium' %}
                              {% elsif select_weight == 'bold' %}
                                      {% render 'icon', icon: 'close-chevron-bold' %}
                              {% endif %}
                          </div>  
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                </form>
                <div class="results-toast results-toast--vertical label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" role="status">
                  <p class="results-toast__text">
                    {%- liquid
                      if results.results_count
                        echo 'templates.search.results_with_count' | t: terms: results.terms, count: results.results_count
                      elsif results.products_count == results.all_products_count
                        if results.products_count == 1
                        echo 'products.facets.found_count_simple.one' | t: count: results.products_count
                        else 
                        echo 'products.facets.found_count_simple.other' | t: count: results.products_count
                        endif
                      else
                        if results.products_count == 1
                        echo 'products.facets.found_count.one' | t: product_count: results.products_count, count: results.all_products_count
                        else
                        echo 'products.facets.found_count.other' | t: product_count: results.products_count, count: results.all_products_count
                        endif
                      endif
                    -%}
                  </p>
                </div>
            </facet-filters-form>
          </div>
      </facet-drawer>
    </div>
  </modal-dialog>
{% endif %}

<modal-dialog class="snippet-facets {% if section.settings.desktop_filter_style == 'horizontal' or settings.navigation_layout == 'header_based' and section.settings.desktop_filter_style == 'vertical' %}only-mobile{% endif %}">
  <div class="facets-container-drawer facets-container-drawer--{{ settings.white_space }} {% if settings.show_lines %}lines{% endif %}">
    <facet-drawer class="mobile-facets__wrapper {% if settings.enable_buttons_zoom %}buttons-zoom{% endif %} {% if settings.center_text %}mobile-facets__wrapper--center{% endif %}">
        <button class="facets__button icon-button button--{{ settings.typography_preset }} {% if settings.typography_preset == 'custom' %}{% if settings.regular_button_size == 'default' %}button-font--default{% else %}{{ settings.regular_button_size }}{% endif %}{% endif %} {{ button_style }}-button {% if button_style == 'link' and settings.enable_animated_underline %}link-button--underline{% endif %} open-popup">
          <span class="facets__button-label icon-button__title">            
            {{ 'products.facets.filter_button' | t }}
          </span>
          <span id="ProductCount--{{ section.id }}"  class="facets__button-label-count">            
            {% liquid 
              assign active_filters = 0
              if section.id contains 'product-grid'
                assign filters = collection.filters
              elsif section.id contains 'search'
                assign filters = search.filters
              endif
            %}
                {%- for filter in filters -%}
                    {%- if filter.active_values.size > 0 or filter.min_value.value != nil or filter.max_value.value != nil -%}
                      {% liquid 
                        assign price_value = 0
                        if filter.min_value.value != nil or filter.max_value.value != nil
                          assign price_value = 1
                        endif 
                        assign active_filters_size = filter.active_values | size
                        assign active_filters = active_filters | plus: active_filters_size | plus: price_value
                        %}
                    {%- endif -%}
                {%- endfor -%}

                {%- unless active_filters == 0 -%}
                  <div class="results-count results-count--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">{{ active_filters }}</div>
                {%- endunless -%}
          </span>
          <span class="facets__button-icon">
            {% if secondary_weight == 'regular' %}
                {% render 'icon', icon: 'filter-regular' %}
            {% elsif secondary_weight == 'medium' %}
                {% render 'icon', icon: 'filter-medium' %}
            {% elsif secondary_weight == 'bold' %}
                {% render 'icon', icon: 'filter-bold' %}
            {% endif %}
          </span>
        </button>
        {% comment %} <div class="overlay"></div> {% endcomment %}
        <div class="filter-form popup-wrapper">
          <div class="scroll-area hide-scrollbar">
            <div class="button-wrapper {% if settings.enable_buttons_zoom %}buttons-zoom{% endif %}">
              <button type="button" class="button-close close-popup">
                {% if close_weight == 'regular' %}
                    {% render 'icon', icon: 'cross-regular' %}
                {% elsif close_weight == 'medium' %}
                    {% render 'icon', icon: 'cross-medium' %}
                {% elsif close_weight == 'bold' %}
                    {% render 'icon', icon: 'cross-bold' %}
                {% endif %}
              </button>
            </div>
            <facet-filters-form>
              <div class="page-width">
                <div class="active-facets-mobile ">
                  <div class="product-count label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" role="status">
                    <p class="product-count__text">
                      {%- liquid
                        if results.results_count
                          if results.results_count == 1
                            echo 'templates.search.results_with_count.one' | t: terms: results.terms, count: results.results_count
                          else
                            echo 'templates.search.results_with_count.other' | t: terms: results.terms, count: results.results_count
                          endif
                        elsif results.products_count == results.all_products_count
                          if results.products_count == 1
                            echo 'products.facets.product_count_simple.one' | t: count: results.products_count
                          else
                            echo 'products.facets.product_count_simple.other' | t: count: results.products_count
                          endif
                        else
                          if results.products_count == 1
                          echo 'products.facets.product_count.one' | t: product_count: results.products_count, count: results.all_products_count
                          else
                          echo 'products.facets.product_count.other' | t: product_count: results.products_count, count: results.all_products_count
                          endif
                        endif
                      -%}
                    </p>
                  </div>
                      <div class="active-facets {{ total_active_values }} {{ price_range_active }} {% if total_active_values == 0 and price_range_active == false %} hidden{% endif %}">
                        {%- for filter in results.filters -%}
                          {%- for value in filter.active_values -%}
                            <facet-remove>
                              <a class="active-facets__button hover-opacity button--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" href="{{ value.url_to_remove }}">
                                {{ filter.label | escape }}: {{ value.label | escape }}
                                {% if secondary_weight == 'regular' %}
                                    {% render 'icon', icon: 'cross-regular' %}
                                {% elsif secondary_weight == 'medium' %}
                                    {% render 'icon', icon: 'cross-medium' %}
                                {% elsif secondary_weight == 'bold' %}
                                    {% render 'icon', icon: 'cross-bold' %}
                                {% endif %}
                              </a>
                            </facet-remove>
                          {%- endfor -%}
                          {% if filter.type == "price_range" %}
                            {%- if filter.min_value.value != nil or filter.max_value.value != nil -%}
                              <facet-remove>
                                <a class="active-facets__button hover-opacity button--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" href="{{ filter.url_to_remove }}">
                                  {{ filter.label | escape }}: <span class="ltr-text">{% if filter.min_value.value != nil %}{{ filter.min_value.value | money_without_trailing_zeros }}{% else %}{{ 0 | money_without_trailing_zeros }}{% endif %}—{% if filter.max_value.value != nil %}{{ filter.max_value.value | money_without_trailing_zeros }}{% else %}{{ filter.range_max | money_without_trailing_zeros }}{% endif %}</span>
                                  {% if secondary_weight == 'regular' %}
                                      {% render 'icon', icon: 'cross-regular' %}
                                  {% elsif secondary_weight == 'medium' %}
                                      {% render 'icon', icon: 'cross-medium' %}
                                  {% elsif secondary_weight == 'bold' %}
                                      {% render 'icon', icon: 'cross-bold' %}
                                  {% endif %}
                                </a>
                              </facet-remove>
                            {%- endif -%}
                          {% endif %}
                        {%- endfor -%}
                        <facet-remove class="facets__clear-all">
                          <a href="{{ results_url }}" class="active-facets__button hover-opacity active-facets__button--clear dim button--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">
                            {{ 'products.facets.clear_all' | t }}
                          </a>
                        </facet-remove>
                      </div>
                </div>
              </div>
              <form id="FacetFiltersFormMobile" class="mobile-facets">
                <div class="facets__wrapper">
                  <div class="sorting">
                    <div class="facet-filters__field">
                      <label class="filter-group-label filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="SortBy">{{ 'products.facets.sort_by_label' | t }}</label>
                      <div class="select">
                        {%- assign sort_by = results.sort_by | default: results.default_sort_by -%}
                        <select name="sort_by" class="facet-filters__sort select__select caption-large sorting__select input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" id="SortBy" aria-describedby="a11y-refresh-page-message">
                          {%- for option in results.sort_options -%}
                            <option value="{{ option.value | escape }}"{% if option.value == sort_by %} selected="selected"{% endif %}>{{ option.name | escape }}</option>
                          {%- endfor -%}
                        </select>
                        <div class="select-icon">
                          {% if select_weight == 'regular' %}
                                  {% render 'icon', icon: 'close-chevron-regular' %}
                          {% elsif select_weight == 'medium' %}
                                  {% render 'icon', icon: 'close-chevron-medium' %}
                          {% elsif select_weight == 'bold' %}
                                  {% render 'icon', icon: 'close-chevron-bold' %}
                          {% endif %}
                      </div>  
                      </div>
                    </div>
                  </div>
                </div>
                <div class="mobile-facets__inner">
                  <div class="mobile-facets__main">
                    {%- if enable_filtering -%}
                      {%- for filter in results.filters -%}
                        {% liquid 
                          assign presentation = filter.presentation | default: default_presentation
                        %}
                        {% case filter.type %}
                        {% when 'boolean' or 'list' %}
                          {% liquid 
                            assign color_swatches_label_1 = settings.color_swatches_label_1 | downcase 
                            assign color_swatches_label_2 = settings.color_swatches_label_2 | downcase 
                            assign color_swatches_label_3 = settings.color_swatches_label_3 | downcase 
                            assign thumbnail_swatches_label_1 = settings.thumbnail_swatches_label_1 | downcase
                            assign thumbnail_swatches_label_2 = settings.thumbnail_swatches_label_2 | downcase
                            assign thumbnail_swatches_label_3 = settings.thumbnail_swatches_label_3 | downcase
                          
                            assign filter_label = filter.label | downcase
                            assign swatch = false
                            assign thumbnail = false

                            if presentation == 'image'
                                  assign thumbnail = true
                            endif
                            if presentation == 'swatch'
                                assign swatch = true
                            endif

                            if presentation == 'text'
                              if filter_label == color_swatches_label_1 or filter_label == color_swatches_label_2 or filter_label == color_swatches_label_3
                                assign swatch = true
                              endif
                              if filter_label == thumbnail_swatches_label_1 or filter_label == thumbnail_swatches_label_2 or filter_label == thumbnail_swatches_label_3
                                assign thumbnail = true
                              endif
                              if swatch and thumbnail
                                assign swatch = false
                                assign thumbnail = true
                              endif
                            endif
                            
                            assign tooltip = false
                            if swatch and settings.color_swatches_layout == 'grid'
                              assign tooltip = true
                            elsif thumbnail and settings.show_thumbnail_name == false
                              assign tooltip = true
                            endif
                          %}
                          <accordion-block data-index="mobile-{{ forloop.index }}">
                            <div class="accordion-toggle no-js-hidden filter-group mobile-facets__details js-filter" id="Details-mobile-{{ filter.param_name | escape }}-{{ section.id }}" data-index="mobile-{{ forloop.index }}" tabindex="0">
                              <div class="mobile-facets__summary accordion__summary" role="button" tabindex="0">
                                <span class="summary__title">
                                  <span class="summary__title-name filter-group-label filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">
                                    <span class="mobile-facets__title">{{ filter.label | escape }}</span>
                                    <div class="filter-group-icon filter-group-icon--plus">
                                      {% if weight_accordion_title == 'regular' %}
                                              {% render 'icon', icon: 'plus-regular' %}
                                      {% elsif weight_accordion_title == 'medium' %}
                                              {% render 'icon', icon: 'plus-medium' %}
                                      {% elsif weight_accordion_title == 'bold' %}
                                              {% render 'icon', icon: 'plus-bold' %}
                                      {% endif %}
                                    </div>
                                    <div class="filter-group-icon filter-group-icon--minus">
                                      {% if weight_accordion_title == 'regular' %}
                                              {% render 'icon', icon: 'minus-regular' %}
                                      {% elsif weight_accordion_title == 'medium' %}
                                              {% render 'icon', icon: 'minus-medium' %}
                                      {% elsif weight_accordion_title == 'bold' %}
                                              {% render 'icon', icon: 'minus-bold' %}
                                      {% endif %}
                                    </div>
                                  </span>
                                </span>
                              </div>
                              <div class="accordion__panel mobile-facets__submenu filter-group-display">
                                {%- if filter.operator == 'AND' and section.settings.filter_and_operator_subtitle != blank -%}
                                  <span class="facets__and-helptext {% if settings.dim_text %}dim{% endif %} filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">
                                    {{ section.settings.filter_and_operator_subtitle }}
                                  </span>
                                {%- endif -%}
                                {% liquid 
                                  assign grid_view = false
                                  if swatch and settings.color_swatches_layout == 'grid'
                                    assign grid_view = true
                                  elsif thumbnail
                                    assign grid_view = true
                                  endif
                                %}
                                <ul class="accordion__content mobile-facets__list list-unstyled {% if grid_view %}filter-group-display__grid{% else %}filter-group-display__list{% endif %}" role="list">
                                  {%- for value in filter.values -%}
                                    {%- liquid
                                      assign is_color = false
                                      if swatch or thumbnail
                                        assign swatch_file_extension = 'png'
                                        assign swatch_trigger = 'products.product.color_swatch_tag' | t | downcase
                                      endif
                                    
                                      if presentation == 'text'
                                        assign color_file_name = value.value | replace: ' ', '_' | append: '.' | append: swatch_file_extension
                                        assign color_value = value.value | replace: ' ', '_' | downcase
                                        assign color_swatch_fallback = value.value | downcase | split: ' ' | last
                                        assign img_file = images[color_file_name] | downcase
                                        assign color_image = images[color_file_name] | split: '/' | last | file_img_url: '100x100' | prepend: 'https:' | split: '?' | first
                                      endif
                                      assign swatch_value = null
                                      if presentation == 'swatch'
                                        if value.swatch.image
                                          assign image_url = value.swatch.image | image_url: width: 50
                                          assign swatch_value = 'url(' | append: image_url | append: ')'
                                          assign swatch_focal_point = value.swatch.image.presentation.focal_point
                                        elsif value.swatch.color
                                          assign swatch_value = 'rgb(' | append: value.swatch.color.rgb | append: ')'
                                        endif
                                      endif
                                      if presentation == 'image'
                                        assign image_url = value.image | image_url: width: 300
                                        assign swatch_value = 'url(' | append: image_url | append: ')'
                                      endif
                                    -%}
                                    <li class="mobile-facets__item list-menu__item filter-group-display__list-item {% if forloop.index > 10 %}show-more-item hidden{% endif %}{% if thumbnail %} mobile-facets__item--thumbnail{% endif %}">
                                      <label for="Filter-{{ filter.label | escape }}-mobile-{{ forloop.index }}" class="mobile-facets__label{% if value.count == 0 and value.active == false %} mobile-facets__label--disabled{% endif %} filter-group-option input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %} {% if thumbnail %}filter-group-option--thumbnail{% endif %}" {% if tooltip %}title="{{ value.label | escape }}"{% endif %}>
                                        <input class="mobile-facets__checkbox {% if swatch %} mobile-facets__swatch{% elsif thumbnail %}{% if settings.thumbnail_image_ratio == 'original' %} ratio-square{% else %} ratio-{{ settings.thumbnail_image_ratio }}{% endif %} mobile-facets__thumbnail{% if settings.enable_shadow %} mobile-facets__thumbnail--shadow{% endif %}{% unless settings.show_thumbnail_name %} no-label{% endunless %}{% endif %}{% if thumbnail and filter.presentation != 'swatch' %} mobile-facets__thumbnail--options{% unless settings.show_thumbnail_name %} no-label{% endunless %}{% endif %}
                                          {% if presentation == 'swatch' or presentation == 'image' %} filter-from-app{% endif %}"
                                          type="checkbox"
                                          name="{{ value.param_name }}"
                                          value="{{ value.value }}"
                                          id="Filter-{{ filter.label | escape }}-mobile-{{ forloop.index }}"
                                          {% if value.active %}checked="checked"{% endif %}
                                          {% if value.count == 0 and value.active == false %}disabled="disabled"{% endif %}
                                          {%  if presentation == 'swatch' or presentation == 'image' and value.image %}style="--swatch--background: {{ swatch_value }};{{ value.swatch.color.first }};--swatch-focal-point: {% if swatch_focal_point %}{{ swatch_focal_point }}{% else %}center{% endif %};"{% else %}
                                          {% if swatch or grid_view %}style="{% if images[color_file_name] != blank and img_file contains color_value %}--swatch-background-image: url({{ color_image }});{% else %} --swatch-background-color: {{ color_swatch_fallback | default: 'white' }};{% endif %}"{% endif %}{% endif %}
                                        />
                                          {% if swatch and settings.color_swatches_layout == 'list' %}
                                            <span class="mobile-facets__swatch--svg mobile-facets__swatch--svg-list">{% render 'icon-accordion-2', icon: 'short-checkmark' %}</span>
                                          {% elsif swatch and settings.color_swatches_layout == 'grid' %}
                                            <span class="mobile-facets__swatch--svg mobile-facets__swatch--svg-grid">{% render 'icon-accordion-2', icon: 'swatch-checkmark' %}</span>
                                          {% endif %}
                                          {% unless tooltip %}<span class="swatch__label">{{ value.label | escape }} ({{ value.count }})</span>{% endunless %}
                                      </label>
                                    </li>
                                  {%- endfor -%}
                                </ul>
                                {% if filter.values.size > 10 %}
                                  <show-more-button>
                                    <button
                                      class="button-show-more no-js-hidden"
                                      id="Show-More-{{ forloop.index }}-{{ section.id }}"
                                      type="button">
                                      <span class="label-show label-show--more label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %} dim">{{ 'products.facets.show_more' | t -}}</span>
                                      <span class="label-show label-show--less hidden label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %} dim">{{ 'products.facets.show_less' | t -}}</span>
                                    </button>
                                  </show-more-button>
                                {% endif %}
                              </div>
                            </div>
                          </accordion-block>
                        {% when 'price_range' %}
                          <accordion-block data-index="mobile-{{ forloop.index }}">
                            {% liquid
                              assign currencies_using_comma_decimals = 'ANG,ARS,BRL,BYN,BYR,CLF,CLP,COP,CRC,CZK,DKK,EUR,HRK,HUF,IDR,ISK,MZN,NOK,PLN,RON,RUB,SEK,TRY,USD,UYU,VES,VND' | split: ','
                              assign uses_comma_decimals = false
                              if currencies_using_comma_decimals contains cart.currency.iso_code
                                assign uses_comma_decimals = true
                              endif
                            %}
                            <div class="accordion-toggle no-js-hidden filter-group mobile-facets__details js-filter" id="Details-mobile-{{ filter.param_name | escape }}-{{ section.id }}" data-index="mobile-{{ forloop.index }}" tabindex="0">
                              <div class="mobile-facets__summary accordion__summary" role="button">
                                <span class="summary__title">
                                  <span class="summary__title-name filter-group-label filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">{{ filter.label | escape }}
                                  <div class="filter-group-icon filter-group-icon--plus">
                                    {% if weight_accordion_title == 'regular' %}
                                            {% render 'icon', icon: 'plus-regular' %}
                                    {% elsif weight_accordion_title == 'medium' %}
                                            {% render 'icon', icon: 'plus-medium' %}
                                    {% elsif weight_accordion_title == 'bold' %}
                                            {% render 'icon', icon: 'plus-bold' %}
                                    {% endif %}
                                  </div>
                                  <div class="filter-group-icon filter-group-icon--minus">
                                    {% if weight_accordion_title == 'regular' %}
                                            {% render 'icon', icon: 'minus-regular' %}
                                    {% elsif weight_accordion_title == 'medium' %}
                                            {% render 'icon', icon: 'minus-medium' %}
                                    {% elsif weight_accordion_title == 'bold' %}
                                            {% render 'icon', icon: 'minus-bold' %}
                                    {% endif %}
                                  </div>
                                  </span>
                                </span>
                              </div>
                              <div class="accordion__panel mobile-facets__submenu">
                                <div class="facets__price">
                                  <div class="fields">
                                    <div class="field">
                                      <label class="field__label filter-group-option label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="Mobile-Filter-{{ filter.label | escape }}-GTE">{{ 'products.facets.from' | t }}</label>
                                      <div class="field__item" dir="ltr">
                                        <span class="field__currency input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">{{ cart.currency.symbol }}</span>
                                        <input class="field__input input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}"
                                          name="{{ filter.min_value.param_name }}"
                                          id="Mobile-Filter-{{ filter.label | escape }}-GTE"
                                          type="number"
                                          dir="ltr"
                                          placeholder="0"
                                          min="0"
                                          step="0.01"
                                          max="{{ filter.range_max | divided_by: 100.0 | ceil }}"
                                          {%- if filter.min_value.value -%}
                                            {%- if uses_comma_decimals %}
                                              value="{{ filter.min_value.value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"
                                            {%- else -%}
                                              value="{{ filter.min_value.value | money_without_currency | replace: ',', '' }}"
                                            {% endif -%}
                                          {%- endif -%}
                                        />
                                      </div>
                                    </div>
                                    <div class="field">
                                      <label class="field__label filter-group-option label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="Mobile-Filter-{{ filter.label | escape }}-LTE">{{ 'products.facets.to' | t }}</label>
                                        <div class="field__item" dir="ltr">
                                          <span class="field__currency input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}">{{ cart.currency.symbol }}</span>
                                          <input class="field__input input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}"
                                            name="{{ filter.max_value.param_name }}"
                                            id="Mobile-Filter-{{ filter.label | escape }}-LTE"
                                            type="number"
                                            dir="ltr"
                                            placeholder="{{ filter.range_max | divided_by: 100.0 | ceil }}"
                                            min="0"
                                            step="0.01"
                                            max="{{ filter.range_max | divided_by: 100.0 | ceil }}"
                                            {%- if filter.max_value.value -%}
                                              {%- if uses_comma_decimals %}
                                                value="{{ filter.max_value.value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"
                                              {%- else -%}
                                                value="{{ filter.max_value.value | money_without_currency | replace: ',', '' }}"
                                              {% endif -%}
                                            {%- endif -%}
                                          />
                                        </div>
                                    </div>
                                  </div>
                                </div>
                              </div>
                            </div>
                          </accordion-block>
                        {% endcase %}
                      {%- endfor -%}
                    {%- endif -%}
                    {%- if enable_sorting -%}
                      <div class="mobile-facets__details js-filter{% if filter_type == 'drawer' %} medium-hide large-up-hide{% endif %}" id="Details-mobile-{{ filter.param_name | escape }}-{{ section.id }}" data-index="mobile-{{ forloop.index }}">
                        <div class="mobile-facets__summary">
                          <div class="mobile-facets__sort">
                            <label class="filter-group-label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" for="SortBy-mobile">{{ 'products.facets.sort_by_label' | t }}</label>
                            <div class="select">
                              <select name="sort_by" class="select__select input--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" id="SortBy-mobile" aria-describedby="a11y-refresh-page-message">
                                {%- for option in results.sort_options -%}
                                  <option value="{{ option.value | escape }}"{% if option.value == sort_by %} selected="selected"{% endif %}>{{ option.name | escape }}</option>
                                {%- endfor -%}
                              </select>
                              <div class="select-icon">
                                {% if select_weight == 'regular' %}
                                        {% render 'icon', icon: 'close-chevron-regular' %}
                                {% elsif select_weight == 'medium' %}
                                        {% render 'icon', icon: 'close-chevron-medium' %}
                                {% elsif select_weight == 'bold' %}
                                        {% render 'icon', icon: 'close-chevron-bold' %}
                                {% endif %}
                            </div>  
                            </div>
                          </div>
                        </div>
                      </div>
                    {%- endif -%}
                  </div>
                  {%- if search.performed -%}<input type="hidden" name="q" value="{{ search.terms | escape }}"/>{%- endif -%}
                  {%- if collection.current_vendor or collection.current_type -%}<input type="hidden" name="q" value="{{ collection.current_vendor | escape }}{{ collection.current_type | escape }}"/>{%- endif -%}
                </div>
                {%- if results.terms -%}
                  <input type="hidden" name="q" value="{{ results.terms | escape }}"/>
                  <input name="options[prefix]" type="hidden" value="last"/>
                {%- endif -%}
                {%- if results.current_vendor or results.current_type -%}
                  <input type="hidden" name="q" value="{{ results.current_vendor | escape }}{{ results.current_type | escape }}"/>
                {%- endif -%}
              </form>
              <div class="results-toast results-toast--drawer label--{{ settings.typography_preset }}{% if uppercase_body_s %} uppercase{% endif %}{% if bolder_body_s %} bolder-font{% endif %}" role="status">
                <p class="results-toast__text">
                  {%- liquid
                    if results.results_count
                      echo 'templates.search.results_with_count' | t: terms: results.terms, count: results.results_count
                    elsif results.products_count == results.all_products_count
                      if results.products_count == 1
                      echo 'products.facets.found_count_simple.one' | t: count: results.products_count
                      else 
                      echo 'products.facets.found_count_simple.other' | t: count: results.products_count
                      endif
                    else
                      if results.products_count == 1
                      echo 'products.facets.found_count.one' | t: product_count: results.products_count, count: results.all_products_count
                      else
                      echo 'products.facets.found_count.other' | t: product_count: results.products_count, count: results.all_products_count
                      endif
                    endif
                  -%}
                </p>
              </div>
            </facet-filters-form>
          </div>
        </div>
    </facet-drawer>
  </div>
</modal-dialog>